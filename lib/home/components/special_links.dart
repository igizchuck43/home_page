import 'package:flutter/material.dart';

class SpecialLinks extends StatelessWidget {
  const SpecialLinks({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Padding(
            padding: const EdgeInsets.symmetric(horizontal: 20),
        ),
      ],
    );
  }
}
